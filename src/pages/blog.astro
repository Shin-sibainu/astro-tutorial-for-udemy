---
const pageTitle = "My Astro Learning Blog";
// const allPosts = await Astro.glob("../pages/posts/*.md");

import BaseLayout from "../layouts/BaseLayout.astro";
import BlogPost from "../components/BlogtPost.astro";
import { getCollection } from "astro:content";
import BlogtPost from "../components/BlogtPost.astro";

const allPosts = await getCollection("posts");
---

<BaseLayout pagetTitle={pageTitle}>
  <p>ここには、私がAstroを学んでいく旅の様子を投稿していきます。</p>

  <ul>
    {
      allPosts.map((post) => (
        <BlogtPost url={`/posts/${post.slug}`} title={post.data.title} />
      ))
    }
  </ul>
</BaseLayout>
